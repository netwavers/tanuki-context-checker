# 🐾 生成AI文章チェッカー (Tanuki Context Checker) ユーザーマニュアル

**バージョン**: 0.2.0  
**対象者**: SeaCafe オーナー（かぜまる様）、開発担当者、運用レビューア  

---

## 1. 概要とコンセプト

「生成AI文章チェッカー (Tanuki Context Checker)」は、入力された文章が生成AIによって出力されたものか、人間によって書かれたものかを**構造的・意味的**に測定・判定するツールの実装です。

### 💡 従来のチェッカーとの違い（目的関数）
* **従来のチェッカー**: 単純な単語マッチングや文章の「破綻・文法エラー」を検出。
* **本ツール**: 文章に**「人間らしいゆらぎ（思考の痕跡：脱線・自己訂正・力点の偏り・一人称体験）」が欠如している度合い（均質化の度合い）**を測定。
  * 「破綻がなく非常にきれいに整理されすぎている文章」をAI生成の正のシグナルとして検出します。
  * 仕様書や技術文書などの整った文章に対する偽陽性を防ぐため、**ドメイン補正機構**を備えています。

---

## 2. 動作環境

* **Python**: 3.9 以上
* **依存パッケージ**: 標準ライブラリのみで動作可能（外部APIや重厚なMLライブラリ非依存で高速動作）

---

## 3. CLI（コマンドライン）での使用方法

プロジェクトルート（`/home/tanuki/PyProjects/tanuki- context- checker`）にて、以下のコマンドで実行できます。

### 3.1 基本的な使い方（テキストを直接指定）

```bash
python3 -m tanuki_checker "ここに解析したい文章を入力します。一人称の体験や脱線表現が含まれているかをチェックします。"
```

### 3.2 ファイルを指定して解析 (`-f` / `--file`)

テキストファイル（`.txt` や `.md` など）を指定して解析します。

```bash
python3 -m tanuki_checker -f "生成AI文章チェッカー 要求定義書（仕様）.md"
```

### 3.3 ドメイン補正の指定 (`-d` / `--domain`)

文章の種別に応じてドメインを指定することで、構造化による偽陽性を防ぎ正確に判定します。

```bash
python3 -m tanuki_checker -f my_spec.md -d technical_doc
```

#### 指定可能なドメイン一覧
| ドメイン名 | 説明 | 補正特性 |
| :--- | :--- | :--- |
| `general` *(デフォルト)* | 一般散文・汎用テキスト | 標準的な重み付け |
| `technical_doc` / `spec` | 技術仕様書・設計書 | 箇条書きや構造化による高構造スコアを補正し偽陽性を抑制 |
| `academic_paper` | 学術論文・レポート | 受動態や定型構文を考慮し補正 |
| `report` | 業務・調査レポート | 中程度の構造化を許容 |
| `blog` | 技術・個人ブログ | 個人の経験談や一定の口語表現を期待 |
| `essay` | 散文・エッセイ・雑記 | ゆらぎの欠如に対して厳しく判定 |
| `novel` | 小説・文芸・創作 | （参考値/実験的）会話文・台詞主体の展開や文体ゆらぎを考慮 |

### 3.4 JSON形式での出力 (`--json`)

他のプログラムや自動化スクリプトと連携する場合、`--json` オプションで構造化データとして取得できます。

```bash
python3 -m tanuki_checker -f sample.md -d blog --json
```

### 3.5 AI校正用データの出力 (`--proofread-json` / `--proofread-prompt`)

検出したAI均質化シグナルに基づき、LLM（Gemini/GPT/Claude）にそのまま入力してAI臭さを除去・自然に校正させるためのプロンプトおよび指示データを出力します。

```bash
# AI校正用詳細JSONデータを出力
python3 -m tanuki_checker -f sample.txt --proofread-json

# LLMのコンテキストウィンドウに直接貼り付け可能なプロンプトテキストを出力
python3 -m tanuki_checker -f sample.txt --proofread-prompt
```

---

## 4. Python API での使用方法

Pythonスクリプトからライブラリとして呼び出す場合の最小コード例です。

```python
from tanuki_checker.pipeline import TanukiContextChecker

# 1. チェッカーの初期化
checker = TanukiContextChecker()

# 2. 文章の解析実行 (テキスト, ドメイン)
text = """
本稿では、生成AI文章の構造的特徴について解説します。
第一に、構文木のエントロピーが低く抑えられます。
第二に、段落の長さが均一になる傾向があります。
"""

report = checker.analyze_text(text, domain="technical_doc")

# 3. 結果の参照
print(f"総合スコア: {report.overall_score:.1f} / 100.0")
print(f"判定区分  : {report.classification}")
print(f"判定信頼度: {report.confidence.value}")

# 層別スコア
print("層別スコア:", report.layer_scores)
# "surface": 表層, "lexical": 語彙, "structural": 構造, "flow": フロー

# 検出根拠（説明可能性）
for exp in report.evidence_explanations:
    print(" 根拠:", exp)
```

---

## 5. 結果レポートとスコアの見方

出力されるレポートには、以下の項目が含まれます。

```text
============================================================
🐾 Tanuki Context Checker - 解析レポート
============================================================
総合AI生成スコア : 34.2 / 100.0
判定区分         : 混在・要確認 (人間補正またはAIと人間の共同編集)
判定信頼度       : HIGH
適用ドメイン     : technical_doc
------------------------------------------------------------
【層別スコア (100点満点中、高いほどAI均質化傾向)】
  • 表層スコア (Surface)   : 64.8
  • 語彙スコア (Lexical)   : 0.4
  • 構造スコア (Structural): 40.1
  • フロースコア (Flow)    : 75.4
------------------------------------------------------------
【主要な検出根拠・説明 (Explainability)】
  1. Markdownプロンプト残留/過剰強調が検出されました (異常スコア: 1.00)
  2. 脱線・自己訂正・一人称体験が欠如しており、AI特有の均質で機械的な文章フローです
  3. ドメイン補正 (technical_doc) を適用し、仕様書・報告書等の構造化に伴うスコアを最適補正しました
============================================================
```

### 5.1 総合AI生成スコア (0.0 ～ 100.0)
* **0.0 ～ 30.0**: **人間らしいゆらぎが強い (HUMAN_FLUCTUATION_STRONG)**
  * 人間が自然に執筆した文章である可能性が極めて高い状態です。
* **30.1 ～ 69.9**: **混在・要確認 (MIXED_NEEDS_REVIEW)**
  * 人間がAI出力を編集した文章、またはAIが人間の下書きを清書した共同編集状態です。
* **70.0 ～ 100.0**: **AI生成の可能性が高い (AI_HOMOGENEOUS_HIGH)**
  * 思考のゆらぎや力点の偏りが見られず、AI特有の均一化された文章です。

### 5.2 4つの解析層 (Layer Scores)
1. **表層スコア (Surface)**: 消し忘れMarkdown（````` や `ご参考になりましたら幸いです` 等）、emダッシュ過剰などを検出。
2. **語彙スコア (Lexical)**: AI頻出表現（「〜が重要です」「総合的に見て」等）の密度、ヘッジ表現、N-gramエントロピー。
3. **構造スコア (Structural)**: 依存構造ASTの深さ分散、段落長・文長の変動係数（均一すぎる段落長は高スコア）。
4. **フロースコア (Flow)**: 話題の脱線、自己訂正（「〜だが、実は」「〜と思われたが」）、一人称・具体体験の密度（欠如していると高スコア）。

---

## 6. テスト・精度検証の実行

リポジトリに同梱されている単体テストおよびベンチマーク監査を実行する方法です。

```bash
# 全単体テストの実行
python3 -m unittest discover tests

# Stage 3 精度・パフォーマンス監査レポートの実行
python3 tests/audit_stage3_performance_and_accuracy.py
```

---

## 7. 既知の限界と注意事項（小説・文芸テキスト等）

本システムは、一般的散文・技術文書・エッセイ・レポート等の構造統計およびフローゆらぎ解析において最適化されていますが、以下の対象については**判定精度が低下する既知の課題・仕様上の境界条件**が存在します。

### 7.1 小説・文芸・創作テキストにおける課題
* **会話文（「〜」）の多用**: 台詞主体の構成では、文長分散や段落長の変動係数が極端な値を示しやすく、構造統計スコアの精度がブレる原因となります。
* **作風・文体の特殊性**: 登場人物視点の一人称記述や倒用・意図的省略表現など、文芸作品特有の表現が「人間のゆらぎ」として過大評価される、あるいはAI特有の均質化表現と誤誤解される場合があります。
* **現在の対応方針**: オーナー方針に基づき**「既知の課題・境界条件」**として位置づけております。小説ドメイン（`-d novel`）は参考値補正として提供しておりますが、運用時はスコアを鵜呑みにせず手動レビューの補助指標としてご利用ください。

---
ご不明な点や追加の調整要望がございましたら、お気軽にお知らせくださいの🐾
